DEPENDENCIES:
- Qt 5 (or 6)
- Lua 5.3
(apt packages: `qt5-common`, `liblua5.3`)

INSTRUCTIONS:
run `qmake`, `make`, outputs to `a.out`

EMACS:
Qt .pro mode: package `qt-pro-mode` (or https://www.emacswiki.org/emacs/qt-pro-mode.el)
